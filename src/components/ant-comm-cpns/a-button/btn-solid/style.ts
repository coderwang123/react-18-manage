import styled from "styled-components";
import StyledCommBtnCustom from "../comm-style";

// 使用styled-components创建一个新的组件，它继承自Ant Design的Button组件
const StyledBtnSolid = styled(StyledCommBtnCustom)``;
export default StyledBtnSolid;
