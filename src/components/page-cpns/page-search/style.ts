import styled from "styled-components";
import { Form } from "antd";

const StyledPageSearch = styled(Form)`
  .item-col {
    padding-left: 15px !important;
    padding-right: 15px !important;
  }
`;

export default StyledPageSearch;
