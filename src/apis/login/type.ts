export declare namespace IApiLogin {
  type TLogin = {
    phone: string;
    code: string;
  };

  type TCode = {
    phone: string;
    imgCode: string;
  };

  type TImgCode = {
    phone: string;
  };
}
