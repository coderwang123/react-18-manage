import type { IPageButton } from "@/components/page-cpns";

export const buttonConfigHandle: IPageButton[] = [
  {
    name: "新增角色",
    code: "add",
    permission: "web:role:add",
    icon: "PlusOutlined"
  }
];
